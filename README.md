# CHB-LG Reports

This repository hosts curated static study reports exported from
`CHB-LG-LEAN` for collaborator review.

The published site is sanitized for public sharing:

- report HTML pages are preserved
- selected plots, images, and videos are preserved
- machine-readable raw artifacts are omitted from the public copy

The current portal entrypoint is [`index.html`](./index.html).
